<!DOCTYPE html>
<html lang="fr">

<%- include('../partial/head', { name: "Inscription...", icon: "favicon" }) %>

<body>

    <div class="form">
        <form method="POST" action="/auth/register">
            
            <h1>Inscription</h1>
            
            <div>
                <label>Nom d'utilisateur :</label>
                <input type="text" name="username" maxlength="20" required>
            </div>
            <% if(error.includes("username")) { %><label class="red"><strong>Ce nom d'utilisateur est déjà pris !</strong></label><br><%}%>
          
            <div>
                <label>Mot de passe :</label>
                <input type="password" name="password" maxlength="20" required>
            </div>

            <div>
                <label>Confirmation :</label>
                <input type="password" name="confirmPassword" maxlength="20" required>
            </div>
            <% if(error.includes("notSimilar")) { %><label class="red"><strong>Les mots de passe sont différents !</strong></label><br><%}%>

            <div><button type="submit">Enregistrer</button></div>
       </form>
    </div>
    
</body>

<script> if("<%= created %>") alert("Vous venez de créer le compte <%= created %> avec succès !"); </script>

</html>